{
  "name": "coastlight-new",
  "version": "1.0.0",
  "description": "The website for the digital agency, coastlight digital.",
  "main": "index.js",
  "scripts": {
    "build:sass": "sass _src/_sass:_src/assets/css",
    "watch:sass": "sass --watch _src/_sass:_src/assets/css",
    "build:eleventy": "npx @11ty/eleventy",
    "debug": "DEBUG=Eleventy* npx @11ty/eleventy",
    "build": "npm run build:sass & ENVIRONMENT='production' npm run build:eleventy",
    "dev": "npx @11ty/eleventy --serve & npm run watch:sass"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "sass": "^1.58.2",
    "slugify": "^1.6.5"
  },
  "dependencies": {
    "clean-css": "^5.3.2"
  }
}
